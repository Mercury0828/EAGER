# DESIGN_DECISIONS — EAGER (eager-dqc)

Append-only decision log. Format: `D## | date | decision | rationale |
alternatives rejected`. Locked decisions are not reopened without an explicit
override entry; forced deviations get a NEW numbered entry, never an edit.
This file is INTERNAL ONLY (excluded from any public artifact release).

D1–D9 mirror the initial decision log in `docs/guide.md` §14 (canonical).

---

D1 | 2026-06-10 | Multi-hop remote gates via fixed shortest-path swapping with q=1; entanglement *generation* is the only stochastic element in v1. | Networking flavor without routing-decision complexity (guide §14). | Adaptive/learned routing; stochastic swapping.

D2 | 2026-06-10 | Static qubit placement; no TP-Comm/migration in v1. | Scope control; limitation paragraph + future work (guide §14, A2). | Mid-circuit migration actions.

D3 | 2026-06-10 | Objective weights α=1.0, β=1.0, γ=0.5 provisional; one calibration pilot in Phase 6, then frozen. | Terms should be same order of magnitude on medium instances (guide §5.1). | Per-instance adaptive weights; tuning weights per experiment.

D4 | 2026-06-10 | Baseline suite per guide §9 (top-venue anchor MICRO'22 aggregation style; home-venue anchor INFOCOM'23 SA placement; learning anchor ICC'25-style flat-state DDQN; expert GreedyJIT; Random-Progressive lower bound; Gurobi exact upper bound). CloudQC demoted to related-work/stretch. | Authority layering (guide §9). | CloudQC as mandatory baseline.

D5 | 2026-06-10 | Codename EAGER; repo `eager-dqc`. The retired prior framework codename (named only in guide §14/D5) must not appear anywhere in this repo. | Lineage constraint (guide §2.3); fresh self-contained formulation. | Reusing prior naming.

D6 | 2026-06-10 | Cat-Comm only; one end-to-end pair (= one stored pair consumed per route link) per remote gate. | Assumption A6 (guide §4.3). | TP-Comm; per-gate multi-pair purification.

D7 | 2026-06-10 | GenEPR(l) = persistent generate-until-success tasking of one free channel; no cancel action in v1. | Smaller action space; matches channel hardware semantics (guide §4.4). | Cancellable tasks; per-slot one-shot attempts.

D8 | 2026-06-10 | Counter-based CRN (Philox/stable hash) for ALL stochastic draws: `outcome(seed, l, c, t)`; mandatory property tests. | Valid common-random-number paired comparisons across policies (guide §6.5). | Stateful sequential RNG (order-dependent, breaks CRN pairing).

D9 | 2026-06-10 | T_budget = 20·(M+N)+200 slots; truncation penalty P_trunc = α·10·(#unfinished gates). | Bounded episodes; a sane policy never truncates (guide §6.1). | Unbounded episodes; per-step timeout heuristics.

---

Session decisions (Phase 0/1A/1B bootstrap):

D10 | 2026-06-10 | Repo rooted at the existing working directory (project dir name `EAGER`); package name `eager-dqc`, import package `eager`, layout exactly per guide §12. Canonical guide moved from repo root to `docs/guide.md`. | Guide §12 prescribes the inner layout, not the outer dir name; the user-placed guide belongs at `docs/guide.md`. | Renaming the working directory (pointless churn).

D11 | 2026-06-10 | Python 3.12.10 venv at `.venv` (standard CPython); Phase 0/1 deps only: numpy, networkx, pyyaml, pandas, pyarrow (+pytest dev extra). torch/torch_geometric/gurobipy deferred to their phases. | Guide §12 requires 3.11+ and user-local env; 3.12 is the system's standard CPython. Minimal deps per session scope. | conda env (a 3.11 miniconda exists but standard venv is leaner); installing GPU/MILP deps now.

D12 | 2026-06-10 | Config schema concretes: `mode: stochastic|deterministic` and `t_ep` live in the hardware config; `T_cut: null` means no cutoff (∞); per-link overrides keyed `"u-v"`; unknown keys are hard errors; link ids = index into the lexicographically sorted edge list; `kappa` accepts a scalar (uniform) or per-QPU list; `ring` requires K≥3 (K=2 ring would duplicate the line edge). | Deterministic mode is a hardware-level semantics switch (guide §5.2); typo-proof validation with helpful errors (Phase 0 scope). | Separate experiment-config file for mode (overkill at this phase); silent ignoring of unknown keys.

D13 | 2026-06-10 | Timing conventions (implements guide §6.1 exactly): slots are 0-indexed; a channel tasked during slot t makes its first draw at the resolve of slot t (deterministic mode: pair lands at end of slot t+t_ep−1); a pair generated at the resolve of slot t is consumable during slots t+1..t+T_cut and expires (waste, charged once, attached to that ADVANCE's reward) at the resolve of slot t+T_cut; ages increment at resolve step (3), so stored pairs always show age ≥ 1 at micro-action time; a gate scheduled in slot t with duration d completes at the resolve of slot t+d−1; its successors become ready with ready_slot = t+d; source gates have ready_slot = 0; makespan T = number of resolved slots when the last gate completes (= ADVANCE count, so Σ ADVANCE rewards = −α·T); truncation when post-increment t > T_budget; remote-gate stall = schedule_slot − ready_slot. | All downstream hand-derivations, golden tests, and metrics need one frozen convention; this is the direct reading of §6.1's resolve order (generation → gate progress → aging → t+1). | Aging before generation (would let pairs expire one slot early); 1-indexed slots.

D14 | 2026-06-10 | Env API: configs are bound at construction (`EagerEnv(hardware, circuit, params)`), `reset(seed)` (re)starts an episode; `step()` on an invalid action raises ValueError with the violated rule; `reset` rejects instances with N > Σκ (unmappable). | Immutable config per env instance is the standard contract; hard errors surface agent bugs that penalize-and-continue would hide. | `reset(config, seed)` re-binding configs per call; silent penalty for invalid actions.

D15 | 2026-06-10 | Fixed action-space enumeration for masks: [Map(q,u) q-major] ++ [Schedule(g)] ++ [GenEPR(l)] ++ [ADVANCE last]; mask is a bool vector of length N·K+M+L+1. | Agents (Phase 5) and DDQN-flat need a stable index contract from day one. | Dict-only action sets (no stable indexing for replay buffers).

D16 | 2026-06-10 | Pair consumption is FIFO oldest-first per link. | Maximizes utilization under cutoff (oldest pairs die first); deterministic and hand-computable. | Youngest-first (provably wasteful under T_cut); random choice (breaks determinism).

D17 | 2026-06-10 | One canonical route per unordered QPU pair {u,v}: computed from the lower-indexed endpoint with lexicographically-smallest shortest node sequence (greedy min-index next hop); both gate-operand orders use the same route. | Pair consumption must be well-defined regardless of operand order (guide §4.3 fixed routing, lexicographic tie-break). | Direction-dependent routes (would make C_comm depend on operand order).

D18 | 2026-06-10 | CRN engine = numpy Philox, per-draw stateless construction: key = (seed mod 2^64, upper seed bits XOR a fixed stream constant), counter = (t, channel, link, 0); `uniform(l,c,t)` is the first float64 of that stream; success iff uniform < p_l. | Same (seed,l,c,t) → same draw regardless of query order or policy, by construction (guide §6.5, D8); numpy Philox is platform-stable. | xxhash→[0,1) (extra dependency); stateful Generator shared across draws (order-dependent).

D19 | 2026-06-10 | Trajectory hash = chained SHA-256 over canonical JSON of (action repr, integer-only obs snapshot, reward as float.hex(), done) per step, seeded with the reset obs. | Cross-process determinism acceptance needs a stable, platform-reproducible fingerprint; float.hex() avoids repr ambiguity. | Python `hash()` (salted per process); pickling (not canonical).

D20 | 2026-06-10 | Tiny scripted demo policies live in `eager/utils/scripted_policies.py` (first-fit mapper + JIT-style generator + eager-generation variant). They are test/demo helpers shared by tests and `scripts/run_episode.py`, explicitly NOT the Phase-2 GreedyJIT baseline (no METIS partitioning, no criticality-ordered list scheduling). | Phase 1 acceptance requires scripted policies; duplicating them in tests and scripts invites drift. Phase gating concerns baselines/agents, not trivial helpers. | Inlining per-test policies (drift); implementing GreedyJIT early (phase violation).

D21 | 2026-06-10 | auto_jit (guide §9.7) implemented at env level, default OFF: when ON, applying ADVANCE first auto-issues GenEPR for per-link deficits of ready-but-pair-blocked remote gates (deficit = blocked demand − stored − busy, capped by free channels and buffer headroom), links ordered by max criticality of the gates they block (then link id), then the slot resolves normally. | §9.1(3) JIT semantics at the moment the agent yields the slot; deficit cap prevents over-tasking; criticality priority per guide. | Auto-provisioning at gate-ready time inside resolve (would generate within the same slot the demand appears, too strong); uncapped tasking.

D22 | 2026-06-10 | Synthetic instance generator ships in Phase 0 (config system completeness: `kind: synthetic` must be loadable/validatable); per-qubit serialization is enforced structurally by deriving the DAG from the gate-list order via last-toucher edges. | Smoke + config validation need buildable instances; the guide's serialization requirement holds by construction rather than by post-hoc check. | Stub generator until Phase 1A; explicit DAG edges in YAML (redundant, error-prone).

D23 | 2026-06-10 | `python -m eager.smoke` parses and summarizes configs only (no env dependency). | Phase 0 acceptance is "prints a parsed config"; keeps the entry point stable before the env exists. | Smoke running an episode (couples Phase 0 to 1A).

D24 | 2026-06-10 | CLAUDE.md and all repo files carry no owner names, usernames, or external personal URLs; the startup skill package is referenced generically where needed. | Guide §2.4 double-blind hygiene: no author-identifying strings anywhere in the repo. | Embedding the owner/skill-repo identity in CLAUDE.md (template default, rejected for this repo).

D25 | 2026-06-10 | Criticality (features + provisioning priority) = longest path to sink in gate counts over the static DAG, computed once per instance. | Duration-aware criticality depends on the mapping (remote=2 slots), which is a decision variable; the static quantity is well-defined pre-mapping (guide §6.2 normalizes it anyway). | Duration-weighted criticality recomputed per mapping (circular, costlier).

D26 | 2026-06-11 | auto_jit acceptance includes a negative control: the same Map+Schedule-only policy on the same instance/seed must TRUNCATE with auto_jit=OFF (and generate zero pairs). | Attributes episode completion causally to the env-level JIT routine rather than to the instance being accidentally easy. | Positive smoke only (cannot distinguish auto_jit working from remote gates being absent).

D27 | 2026-06-11 | Verification tooling: `scripts/run_repeat_suite.py` implements the 10x flaky-bug protocol (subprocess pytest runs with junitxml in a temp dir, per-test pass-count table, non-zero exit unless N/N); `scripts/clean_state_verify.py` implements clean-state verification (fresh clone + fresh venv, episode script before pytest, pytest, episode script after, byte-identical outputs, file-tree snapshot diff excluding interpreter/runner caches). | Guide §11 protocol items must be one-command reproducible at every future phase gate, not ad-hoc shell history. | Manual per-phase shell transcripts (non-reproducible, error-prone).

---

Phase 2 session decisions (authorized by owner 2026-06-11):

D28 | 2026-06-11 | QASMBench panel composition (overrides the aspirational instance names in guide §10.1): real files at actually-available sizes — adder_n4, qaoa_n6, adder_n28, bv_n30, multiplier_n45, dnn_n51, qft_n63, cat_n65, bv_n70, qugan_n71, ghz_n78, ising_n98 — pinned to the upstream commit in qasm/qasmbench/SOURCE_COMMIT.txt, plus a GENERATED supremacy-style n120 (10 layers of seeded random perfect matchings, seed 2027). VQE family excluded: only available as ~3e5-gate multi-MB programs (vqe_uccsd_n28 extracted to M=296,648), beyond simulator-panel scale. | §10.1's qaoa_n14/qft_n50/vqe_n80/qpe_n90/bv_n100 do not exist in QASMBench; using real files at real sizes keeps the paper's benchmark table verifiable against the upstream suite. | Fabricating instances under QASMBench names that the suite does not contain (reviewer-checkable misrepresentation); including the 3e5-gate VQE (panel-breaking runtime).

D29 | 2026-06-11 | GreedyJIT placement partitioner = pure-Python deterministic greedy growth + FM single-move + KL pairwise-swap refinement under per-QPU capacities (src/eager/baselines/partition.py). pymetis has no Windows wheel and fails to build in this environment (no C++ toolchain); KaHyPar likewise ships no Windows wheels. Disclosed in BASELINE_FIDELITY.md; revisit (conda metis or equivalent) before the Phase 3 MHSA-vs-METIS acceptance comparison. | §9.1 needs a capacity-constrained balanced k-way partitioner now; the stand-in honors the same contract (minimize weighted edge cut under caps) and is quality-tested (optimal cuts on chain/two-cluster/complete micro-graphs). | Blocking Phase 2 on a METIS build; conda env switch mid-phase.

D30 | 2026-06-11 | 2q-skeleton extraction semantics (src/eager/expgen/qasm_skeleton.py): every two-qubit INSTRUCTION (cx, cz, cp/cu1, crz, swap, rzz, ...) contributes exactly one model gate on its qubit pair; ccx expands to the textbook 6-CNOT pair sequence, cswap to 8; single-qubit gates, barriers, measurements dropped (A3); in-file gate definitions inlined recursively; unknown gates and classical conditionals raise (nothing silently dropped). | Instruction-level units match how DQC compiler baselines (Autocomm-style) count remote interactions; decomposing swap to 3 CNOTs would triple QFT's gate count on an arbitrary convention. | swap -> 3 CNOTs (inflates M, no fidelity gain for a skeleton model); silently skipping unknown gates (silent coverage loss).

D31 | 2026-06-11 | Extracted skeletons are frozen as explicit-gate YAMLs under configs/circuits/qasmbench/ (committed), with a drift-guard test re-extracting every qasm source and comparing. | Frozen instances make every later phase's numbers reproducible from the repo alone; the guard keeps qasm/ and configs/ from diverging. | Parsing qasm at env-construction time (couples experiments to parser changes).

D32 | 2026-06-11 | Default Phase 2 panel hardware (eager.expgen.hardware.default_panel_hardware): K=4 2x2 grid, kappa_u = ceil(1.25*N/4), guide §10.2 link defaults (p=1/12, W=2, B=8, T_cut=20, w=1), stochastic mode; CRN-paired evaluation with env seeds {0..4} shared across policies. | §11 Phase 2 says "default config" without pinning K; K=4 grid is the §10.2 center point and exercises multi-hop routes. | K=2 (no multi-hop) or K=8 (outside the training envelope) as the default.

D33 | 2026-06-11 | JIT provisioning issues GenEPR up to FREE CHANNELS / BUFFER HEADROOM on every link with blocked demand (literal guide §9.1 reading), for both GreedyJIT and the env auto_jit hook. SUPERSEDES D21's demand-capped issuing (deficit = demand - stored - busy), which was this project's own stricter interpretation; the demand-capped variant remains only in the D20 demo helper. Measured effect: bv_n30 mean J 180 -> 91.3 at p=1/12. | "up to free channels / buffer headroom" bounds the issue count by the validity constraints, not by demand; the saturating expert is also the stronger, fairer JIT anchor. | Keeping the demand cap (weaker expert, non-literal reading); per-gate lookahead provisioning (no longer JIT, would dilute the §9.7 NoProactive ablation).

D34 | 2026-06-11 | Env hot-path optimization: incremental ready/running/unmapped index sets and obs list caches maintained at mutation points; valid_actions/deficit_demand/resolve iterate those sets; _invalid_reason stays the single source of validity truth; a consistency test (tests/unit/test_env_obs_cache.py) re-derives every cache from the source-of-truth gate objects after every micro-action. Full suite runtime 8.1s -> 4.2s; obs cost now O(copy) not O(M) Python attribute walks. | qft_n63 (M=3906) and multiplier_n45 (M=2574) made per-micro-action O(M) scans the panel bottleneck. | Per-policy fast paths bypassing obs (forks trajectory semantics); numpy mirror state (duplicated source of truth).

D35 | 2026-06-11 | FINDING + ESCALATION (owner ruling requested): the §11 Phase 2 criterion "GreedyJIT J strictly < Random-Progressive on all instances" is structurally unsatisfiable at the default p=1/12 on provisioning-throughput-bound serialized circuits. Mechanism, quantified: §9.5's "ADVANCE only when nothing else is valid" makes Random-Progressive execute essentially EVERY valid action every slot — i.e., random placement + schedule-ASAP + ALWAYS-ON generation on every link — an accidental maximally-proactive provisioner whose only true randomness is placement. On per-qubit-serialized remote chains, reactive JIT pays ~1/(2p) slots of generation latency per gate (measured bv_n30 J: greedy 91.3 vs random 81.8 at p=1/12; greedy wins from p=0.2: 58.0 vs 60.5, and decisively by p=0.5; qaoa_n6 crossover ~p=0.5 — dense 6-clique forced across kappa=2 QPUs, 48/54 gates remote, depth 33). This is the paper's §1 trade-off appearing in the baselines, and per guide §15 the regime map is itself content. Phase 2 acceptance is recorded with the per-instance table; the two regime exceptions are strict-xfail tests + a mechanism-guard test (greedy must win at higher p). Proposed amended criterion for owner approval: "zero GreedyJIT truncations on all instances; J(GreedyJIT) < J(Random-Progressive) except on characterized provisioning-throughput-bound instances, where the gap is the documented proactive-provisioning opportunity". Paper implication: Random-Progressive must be presented as an accidental always-on provisioner (or §9.5 revised) in the Phase 6 evaluation. | Honest reporting of a structural surprise beats silently weakening either baseline; the finding strengthens the paper's motivation. | Redefining GreedyJIT with lookahead (dilutes the NoProactive ablation); weakening Random (a baseline defined in the guide); hiding the failures.

D36 | 2026-06-11 | Trace format v1 (src/eager/baselines/traces.py): header (hardware/circuit names, env seed, mode, auto_jit, policy) + action-index sequence in the agent's ActionSpace vocabulary + per-step rewards + final metrics + trajectory SHA-256; states are NOT stored (replay regenerates them deterministically; the Phase 5 IL dataset builder derives (state, action) pairs the same way). Every recorded trace is replay-verified before being written. | Minimal format whose integrity is the trajectory hash; storing obs per step would be ~100x larger and add a second source of truth. | Per-step obs storage; pickle (non-canonical, unsafe).

D37 | 2026-06-11 | Panel-measured D35 exception set at default p=1/12 over 5 CRN-paired seeds: bv_n30 (greedy 82.4 vs random 79.6 — tie-grade, paired wins 2/5), qaoa_n6 (253.9 vs 246.7 — tie-grade, 3/5), qft_n63 (17645.2 vs 14117.9 — decisive, 0/5; the deepest dense serialized instance). GreedyJIT wins the other 10/13 instances by 1.4x-15x with zero truncations everywhere. experiments/phase2_panel.py reports BOTH the guide-§11-as-written criterion (FAIL) and the D35 amended criterion (PASS with characterized exceptions); the parquet + index.json carry the full per-seed table. | The acceptance record must show the exact measured boundary of the finding, not just its existence. | Reporting only the amended criterion (hides the original's status).

D38 | 2026-06-11 | OWNER RULING (resolves the D35/D37 escalation): the amended Phase 2 criterion is ADOPTED — "zero GreedyJIT truncations on all instances; J(GreedyJIT) < J(Random-Progressive) except on characterized provisioning-throughput-bound instances, where the gap is the documented proactive-provisioning opportunity." Phase 2 acceptance = PASS under this criterion. Both baseline definitions stay exactly as in guide §9.1/§9.5; default p=1/12 stays (literature-calibrated, §4.5). Random-Progressive is presented in the paper as an accidental always-on provisioner (BASELINE_FIDELITY disclosure stands); GreedyJIT and Random-Progressive serve as the two extreme anchors of the provisioning axis (lazy+informed-placement vs maximally-eager+random-placement), and EAGER must beat both. A GreedyEager variant (GreedyJIT placement/scheduling + always-on generation) is PLANNED as a Phase 6 baseline addition to answer the "is learning needed over trivially-always-generating" reviewer attack. The strict-xfail + mechanism-guard tests remain as permanent regime sentinels. | Owner approved the executor's recommendation on 2026-06-11 ("按照你推荐的来"). Strengthening JIT would dilute the §9.7 NoProactive ablation; weakening Random or moving p would be benchmark tuning. | Lookahead-JIT redefinition; §9.5 revision; default-p change (all rejected, see D35).

Phase 3 session decisions (authorized by owner 2026-06-11, "继续PHASE3"):

D39 | 2026-06-11 | MHSA implementation spec (src/eager/baselines/mhsa.py): greedy initialization (highest-interaction first, affinity placement) + 4 stages of [SA over capacity-feasible single moves and pairwise swaps; within-stage geometric cooling from a sampled initial temperature; per-stage temperature halving] each followed by a first-improvement local-search descent; objective = remote-gate count (interaction-graph cut) under per-QPU caps; fixed budget 20,000 proposals, reported with results; deterministic per seed. INFOCOM'23 full text is paywalled here — fidelity is to the published structure (local search hybridized with SA, "multistage") + guide §9.2; labeled "MHSA-style". Placement plugged into the §9.1 scheduler via GreedyJITPolicy.placement_fn so comparisons isolate placement. | §9.2 requires the reimplementation now; every unknown constant chosen once and frozen beats silently tuning later. | Blocking on paper acquisition; per-instance budget tuning (unfair).

D40 | 2026-06-11 | AGG adaptation spec (src/eager/baselines/agg.py): public artifact github.com/anbangw/AutoComm inspected; burst detection follows its consecutive_merge analog on per-qubit serialization chains (maximal same-(u,v) remote runs sharing the chain qubit); cat-comm realized as a placement-aware INSTANCE TRANSFORM — burst head keeps (q,x1) remote (1 pair/route link, d_rem), tails rewritten to local gates (x1,xi) on the target QPU — executed by the UNMODIFIED env (no per-method cost code, §12). Deviations (all model-forced, disclosed in BASELINE_FIDELITY): no commutation-based extension; no tp-comm; side-agnostic sharing; anchor-repeat burst break; early shared-qubit release / overlapping one-to-many bursts (affects T only, never pairs). Structural result: chain-form ghz/cat are burst-FREE under min-cut placement; a constructed fan-out GHZ (ghz_fanout_n78, star gates (0,i)) joins the frozen panel to carry the §11 ghz-class acceptance signal; §11's "strictly reduces consumed pairs on burst-heavy circuits" is interpreted as: strict reduction on every burst-CARRYING instance, exact no-op on burst-free ones. | Artifact-first per §9.3; the transform keeps a single cost implementation; the fan-out instance makes the guide's intent testable instead of vacuously false on chain files. | Env-level burst contexts (would leak free aggregation to all policies and the future agent); commutation reordering (outside the frozen-DAG model).

D41 | 2026-06-11 | Partitioner greedy tie-break changed from most-residual-capacity to LOWEST-PART-ID-WITH-ROOM (sequential fill); refinement unchanged. Reason: zero-affinity qubits (star leaves, BV controls) were round-robined across parts — cut-neutral but burst-destroying (target sequences interleave 1,2,3,1,2,3 -> no consecutive runs at all), making the §9.3 baseline vacuous; sequential fill matches the contiguous-register-split mapping convention of the DQC literature (incl. AutoComm's evaluations). Consequences, re-measured: Phase 2 panel REGENERATED under the new placement (evidence refreshed in PHASE_STATUS, results/phase2_panel.parquet overwritten); bv_n30 LEFT the D37 regime-exception set (contiguous placement lifted GreedyJIT past Random at p=1/12 — caught by the strict-xfail sentinel exactly as designed); qaoa_n6 remains the test-subset exception. | A placement-quality-neutral choice should not silently zero out a published baseline's mechanism. | Keeping round-robin (vacuous AGG); AGG-private placement (breaks §9.3's same-placement controlled comparison).

D42 | 2026-06-11 | torch (CPU build, 2.12) added to core dependencies in Phase 3 — required to IMPLEMENT DDQN-flat (§9.4; training itself is Phase 6). CUDA build deferred to Phase 5 (install over the same venv; pyproject's torch>=2.2 is build-agnostic). DDQN-flat spec: per-config FlatFeaturizer (3K + 4L + 4*top_k(=8) + 4 dims), 256x256 MLP, uniform replay storing next-state masks, masked argmax in BOTH action selection and the Double-DQN target, Huber loss, grad-clip 1.0, periodic target sync; agent RNG (torch+numpy) seeded separately from the env CRN. | §9.4 implementation lands this phase per §11; CPU torch (~200MB) keeps fresh-clone installs viable. | CUDA torch now (2.5GB, no Phase 3 benefit); deferring all torch (would push §9.4 out of its phase).

D43 | 2026-06-11 | Authoritative regenerated Phase 2 panel under D41 (14 instances, 5 CRN seeds): GreedyJIT zero truncations everywhere; ordering exceptions = {qft_n63 (17645 vs 14118, 0/5), ghz_fanout_n78 (475 vs 348, 0/5)} — both decisive, both the canonical provisioning-throughput-bound serialized structures; bv_n30 (76.6 vs 79.6, 3/5) and qaoa_n6 (232.0 vs 246.7, 3/5) resolved to tie-grade wins. Test sentinels updated to the 5-seed protocol: hard asserts for adder_n4/bv_n30/qaoa_n6, strict-xfail for ghz_fanout_n78 (qft_n63 covered by the panel, too slow for the test subset). Side observation: adder_n28's sequential-fill greedy placement is a worse local optimum (J 120->317; partitioner cut 35 vs MHSA 3) — the D29 stand-in's weakness, MHSA's strength; revisit with real METIS per D29. | The exception set is placement-dependent; the record must track the authoritative measurement. | Keeping the stale 3-seed exception set (contradicts the regenerated panel).

